input{
    jdbc {
        jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-8.0.28.jar"
        jdbc_driver_class => "com.mysql.cj.jdbc.Driver"
        jdbc_connection_string => "jdbc:mysql://my-database:3306/openrundatabse"
        jdbc_user => "root"
        jdbc_password => "root"
        schedule => "* * * * *"
        use_column_value => "true"
        lowercase_column_names => false # 대소문자 구분하게 하기.
        tracking_column => "createdAt"
        tracking_column_type => "numeric"
        last_run_metadata_path => "./createdAt.txt"
        statement => "select id, title, contents, location, image, fakeData, period, unix_timestamp(createdAt) as createdAt from event where unix_timestamp(createdAt) > :sql_last_value order by createdAt asc" 
    }
}

output {
    elasticsearch {
        hosts => "elasticsearch:9200"
        index => "event"
        manage_template => true
        template => "/usr/share/logstash/template.json"
        template_name => "template_event"
        template_overwrite => true
    }
}